version       = "0.1.0"
author        = "Jules"
description   = "A Nim clone of the ugui SVG-based UI library"
license       = "MIT"
srcDir        = "src"

requires "nim >= 2.0.0"
requires "pixie"
requires "windy"
requires "chroma"
requires "vmath"
# requires "https://github.com/elcritch/figdraw" # We will assume it's available or use local if needed
